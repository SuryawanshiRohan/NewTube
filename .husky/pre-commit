echo "Running pre-commit checks: lint-staged (ESLint + Prettier on staged files)"

# Run lint-staged on staged files
bunx lint-staged || {
  echo "Pre-commit: lint-staged failed. Fix errors and try again.";
  exit 1;
}

echo "Pre-commit checks passed."
